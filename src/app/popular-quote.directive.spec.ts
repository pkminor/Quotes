import { PopularQuoteDirective } from './popular-quote.directive';

describe('PopularQuoteDirective', () => {
  it('should create an instance', () => {
    const directive = new PopularQuoteDirective();
    expect(directive).toBeTruthy();
  });
});
